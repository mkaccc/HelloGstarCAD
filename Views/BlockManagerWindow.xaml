<Window x:Class="HelloGstarCAD.Views.BlockManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="浩辰CAD图块属性管理与沿线编号" Height="600" Width="850" 
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize">
    
    <Window.Resources>
        <!-- 颜色转换器 -->
        <Style x:Key="BlockColorConverter" TargetType="Border">
            <Style.Triggers>
                <DataTrigger Binding="{Binding BlockTypeHint}" Value="门">
                    <Setter Property="Background" Value="#4A90E2"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding BlockTypeHint}" Value="窗">
                    <Setter Property="Background" Value="#50C878"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding BlockTypeHint}" Value="家具">
                    <Setter Property="Background" Value="#FF6B6B"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding BlockTypeHint}" Value="设备">
                    <Setter Property="Background" Value="#9B59B6"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding BlockTypeHint}" Value="图块">
                    <Setter Property="Background" Value="#95A5A6"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- 拖拽排序样式 -->
        <Style x:Key="DragItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Padding="{TemplateBinding Padding}">
                            <!-- 添加拖拽手柄区域 -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 拖拽手柄图标 -->
                                <Border x:Name="DragHandle" Grid.Column="0" Width="20" VerticalAlignment="Stretch" 
                                        Background="Transparent" Cursor="SizeAll"
                                        ToolTip="拖拽排序">
                                    <TextBlock Text="☰" Foreground="#AAA" FontSize="12" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                
                                <!-- 原有内容容器 -->
                                <ContentPresenter Grid.Column="1"/>
                            </Grid>
                        </Border>
                        
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FFF0F8FF"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#FFBBDEFB"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FFE1F5FE"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#FF2196F3"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="1"/>
                            </Trigger>
                            <!-- 移除不存在的 IsDragOver 触发器 -->
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部功能区 -->
        <GroupBox Grid.Row="0" Header="图块选择（按图块名称去重）" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnSelectBlocks" Content="📥 选择图块" Width="120" Height="35" 
                        Margin="5" ToolTip="选择图块（相同名称的图块只添加一次）"/>
                <Button x:Name="BtnClearList" Content="🗑️ 清空列表" Width="100" Height="35" 
                        Margin="5" ToolTip="清除所有已选图块"/>
                <TextBlock VerticalAlignment="Center" Margin="10,0,0,0" Foreground="Green">
                    <Run Text="已选:"/>
                    <Run Text="{Binding Count, ElementName=LbBlocks}" FontWeight="Bold"/>
                    <Run Text="种图块"/>
                </TextBlock>
            </StackPanel>
        </GroupBox>

        <!-- 中间：图块列表 -->
        <GroupBox Grid.Row="1" Header="图块列表管理（修改标题仅用于编号显示）" Margin="0,0,0,10">
            <!-- 图块列表（占满整个区域） -->
            <ListBox x:Name="LbBlocks" Margin="5" 
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ItemsSource="{Binding}"
                     AllowDrop="True"
                     ItemContainerStyle="{StaticResource DragItemStyle}"
                     PreviewMouseLeftButtonDown="LbBlocks_PreviewMouseLeftButtonDown"
                     Drop="LbBlocks_Drop"
                     DragOver="LbBlocks_DragOver"
                     DragLeave="LbBlocks_DragLeave">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="8,6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 左侧：类型图标 -->
                                <Border Grid.Column="0" Width="50" Height="50" 
                                        Style="{StaticResource BlockColorConverter}"
                                        CornerRadius="4" Margin="0,0,12,0" VerticalAlignment="Center">
                                    <TextBlock Text="▦" FontSize="24" Foreground="White" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                               FontWeight="Bold"/>
                                </Border>
                                
                                <!-- 右侧：详细信息 -->
                                <Grid Grid.Column="1" VerticalAlignment="Center">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- 主标题：显示序号+当前值（可修改的标题） -->
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" 
                                                   Foreground="#2C3E50" FontSize="13"/>
                                        <TextBlock Text="（双击修改）" FontSize="10" Foreground="#FF5722" 
                                                   Margin="8,0,0,0" VerticalAlignment="Bottom"/>
                                    </StackPanel>
                                    
                                    <!-- 属性信息：一行显示图块名称和原始属性值 -->
                                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                                        <!-- 图块名称 -->
                                        <TextBlock FontSize="11" Foreground="#666">
                                            <Run Text="图块: "/>
                                            <Run Text="{Binding BlockName}" Foreground="#2980B9"/>
                                            <Run Text="，" Foreground="#666"/>
                                            <Run Text="属性: "/>
                                            <Run Text="{Binding OriginalAttributeValue}" Foreground="#856404"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <!-- 编号规则设置区 -->
        <GroupBox Grid.Row="2" Header="沿线编号规则" Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <Label Content="前缀:" VerticalAlignment="Center" Width="60"/>
                    <TextBox x:Name="TxtPrefix" Width="80" Height="25" Margin="5,0,30,0" Text="No."/>
                    
                    <Label Content="后缀:" VerticalAlignment="Center" Width="60"/>
                    <TextBox x:Name="TxtSuffix" Width="80" Height="25" Margin="5,0,30,0"/>
                    
                    <Label Content="起始数字:" VerticalAlignment="Center" Width="80"/>
                    <TextBox x:Name="TxtStartNum" Width="80" Height="25" Margin="5,0,30,0" Text="1"/>
                    
                    <TextBlock x:Name="TxtPreview" VerticalAlignment="Center" FontStyle="Italic" 
                               Foreground="#666" MinWidth="200">
                        预览: No.1标题
                    </TextBlock>
                </StackPanel>
                
                <!-- 修改：使用标题作为属性值 -->
                <TextBlock FontSize="11" Foreground="Gray" Margin="5,5,0,0">
                    编号格式: [前缀][数字][标题][后缀]，例如: "No.1设备"
                </TextBlock>
            </StackPanel>
        </GroupBox>

        <!-- 底部执行区 -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button x:Name="BtnSelectPath" Content="🛣️ 选择编号线路" Width="140" Height="40" 
                        Margin="5" FontWeight="SemiBold" ToolTip="在CAD中选择多段线作为编号路径"/>
                <TextBlock VerticalAlignment="Center" Margin="10,0,0,0" FontSize="11" Foreground="#666">
                    提示: 编号将按照列表顺序沿线路放置，使用标题作为显示内容
                </TextBlock>
            </StackPanel>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="BtnStartNumbering" Content="🚀 开始编号" Width="120" Height="40" 
                        Margin="5" Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
                <Button x:Name="BtnClose" Content="关闭" Width="80" Height="40" Margin="15,5,5,5"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>