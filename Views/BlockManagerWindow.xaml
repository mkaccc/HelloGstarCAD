<Window x:Class="HelloGstarCAD.Views.BlockManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HelloGstarCAD.Views"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        mc:Ignorable="d"
        Title="图块管理器" Height="450" Width="600" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 搜索框区域 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="SearchTextBox" Width="200" Height="25" VerticalContentAlignment="Center"/>
            <Button Content="搜索" Width="60" Height="25" Margin="10,0,0,0" Click="SearchButton_Click"/>
        </StackPanel>

        <!-- 状态提示区域：绿色数量提示 -->
        <TextBlock Grid.Row="1" x:Name="BlockCountHint"
                   Text="{Binding BlockCountString}"
                   Foreground="Green"
                   Margin="0,0,0,5" />

        <!-- 图块列表，应用拖放行为 -->
        <ListView Grid.Row="2" x:Name="BlockListView" ItemsSource="{Binding BlockItems}">
            <i:Interaction.Behaviors>
                <local:ListViewDragDropBehavior/>
            </i:Interaction.Behaviors>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="120"/>
                    <GridViewColumn Header="位置" DisplayMemberBinding="{Binding Location}" Width="300"/>
                </GridView>
            </ListView.View>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- 底部按钮区域 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="添加选中图块" Width="100" Height="30" Click="AddSelectedBlocksButton_Click"/>
            <Button Content="关闭" Width="60" Height="30" Margin="10,0,0,0" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>